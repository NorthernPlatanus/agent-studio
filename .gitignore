__pycache__/
*.pyc
.venv/
venv/
.env
config/local.yaml
state/
*.sqlite3
*.db
logs/
.pytest_cache/
*.egg-info/
.DS_Store
.langgraph_api/
docs/
# Per-project overlays: real profiles, prompt overrides, project-maps. Root-anchored
# so it never matches config/projects/ (the committed example.yaml stays tracked).
/projects/
*_pat.txt
*.pat
.secrets/

# Operator config and policy, not repo content.
.claude/
CLAUDE.md
